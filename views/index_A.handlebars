<div class="container index">
		<ul class="nav nav-tabs">
				<li id="createdA_tab" class="active"><a href="#">Created Events</a></li>
				<li id="joinedA_tab"><a href="/joinedEvents">Joined Events</a></li>
		</ul>
	
	{{#each this}}
		{{#if created}}
			{{#if notCancelled}}
				<div class="event-container card-panel">
					<a href="/createdEventDetail/{{eventId}}">
						<div class="event">
							<h5>{{name}}</h5>
							<h5>{{startTime}} to {{endTime}}</h5>
							<p>{{description}}</p>
						</div>
					</a>
				</div>
			{{/if}}
		{{/if}}
	{{/each}}
	
	<a href="/createEvents"><button id="createdA_btn" class="btn btn-primary col-xs-12 btn-lg">Create An Event</button></a>
</div>

<script>
	$("#createdA_tab").click(){function(
		ga("send", "event", "createdA_tab", "click");
	)};
	
	$("#createdA_tab").click(){function(
		ga("send", "event", "joinedA_tab", "click");
	)};
	
	$("#createA_btn").click(){function(
		ga("send", "event", "createdA_btn", "click");
	)};
</script>